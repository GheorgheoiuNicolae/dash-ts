// import { Store } from 'redux';
// import { Action, InjectableAction, InjectableDispatch } from './';

// export interface AppStore<S> extends Store<S> {
//   dispatch: InjectableDispatch<Action<any> | InjectableAction<any>>;
// }
